def	query_db(size, length, combine_loci, tag_primers):
#   	size = request.args.get('size', 0, type=int) 
#   	length = request.args.get('length', 0, type=int)
#   	combine_loci = request.args.get('combine_loci', False, type=bool)
#   	tag_primers
	pass
	